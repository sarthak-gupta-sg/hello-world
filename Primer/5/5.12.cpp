#include <iostream>

using namespace std;

int main()
{
    char ch;
    while(cin >> ch)
    {
        if(ch == 'f')
        {
            cin >> ch;
            switch(ch)
            {
                case 'f': case 'l' : case 'i' : 

    return 0;
